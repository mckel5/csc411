// SIZE = width of word
// f    = (w, lsb)
// f_h  = (SIZE - (lsb + w), lsb + w)
// f_l  = (lsb, 0)

use bitpack::bitpack;

fn main() {
    println!("Hello, world!");
}

fn check_laws(word: u64, w: u64, lsb: u64, value: u64, w2: u64, lsb2: u64) {
    bitpack::gets()
}